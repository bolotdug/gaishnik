spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: mflor
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
server:
  servlet:
    context-path: /number